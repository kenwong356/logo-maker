const fs = require('fs');

class Shapes {

    constructor (text, shapeColor, shape, textColor) {
        this.text = text;
        this.shapeColor = shapeColor;
        this.shape = shape;
        this.textColor = textColor; 
    };
};